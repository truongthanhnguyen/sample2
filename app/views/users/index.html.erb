<% provide :title, t("user.all_users") %>
<h1><%= t "user.all_users" %></h1>

<%= will_paginate%>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate%>
